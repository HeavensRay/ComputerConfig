Frontend routes: /components/{component}/{id}

Components: gpu, cpu, motherboard, ram, ssd, pcu

Backend endpoints are normalized:

GET    /api/{component}/{id}     → fetch single component
PUT    /api/{component}/{id}     → update component (photo + price only editable)
DELETE /api/{component}/{id}     → delete component (admin-only)


Comments endpoints (for all components):

GET    /api/Comments/{baseId}    → fetch comments (any logged-in user)
POST   /api/Comments/{baseId}    → create comment (any logged-in user)
DELETE /api/Comments/{id}        → delete comment (admin-only)


AuthContext provides { isAdmin } for admin-only buttons

All users are read-only by default except for admin buttons

Requirements:

Fetch and display component data for given {id} from backend

Layout:

Center: vertical card with all attributes

Right: large image (Photo)

Admin-only buttons:

Edit → small form to change photo and price

Delete → delete component

Expandable comments section:

Fetch GET /api/Comments/{id} for the component

Display comments as centered rectangles

Admin-only delete button per comment

Create comment form visible to all logged-in users

Deleting a comment removes it from local state

Clicking the comment expand toggle should show/hide comments

Navigation:

Clicking a component row in the list page leads here

Generic:

Copilot should generate this pattern for all components using the same layout & logic

No need to hardcode component-specific names

UI:

Minimal, clean, white cards on dark background

Use accent color #13d4c8 for buttons/highlights

Loading & error states:

Show simple “Loading…” text while fetching

Show error messages if fetch fails

Constraints:

React + Vite (JavaScript) only

No external libraries

All data fetching uses fetch

Auth is read from AuthContext (isAdmin) only

Keep code defensive and clean

Do NOT invent any backend endpoints or fields



You are my assistant for a React + Vite frontend for a PC components app. The backend is ASP.NET Core Web API with normalized endpoints. Only frontend work is needed.

Project context:

Frontend routes:

/config → configurations page

/components/{component} → list page

/components/{component}/{id} → detail page

Components: gpu, cpu, motherboard, ram, ssd, pcu

Styling:

Background: #072644

Accent: #13d4c8

Cards: white on dark

Minimal, clean, modern UI

Auth:

JWT stored in localStorage under token

Payload includes { role: "Admin" }

isAdmin boolean derived from JWT

Admins can create, edit, delete; all others read-only

Admin-only UI controls:

Create / Edit / Delete buttons on components

Delete buttons on comments

All users:

Can read component lists and details

Can create comments

Normalized backend endpoints:

Components (all):
  GET    /api/{component}          → list all
  GET    /api/{component}/{id}     → get by id
  POST   /api/{component}          → create
  PUT    /api/{component}/{id}     → update (photo + price only)
  DELETE /api/{component}/{id}     → delete


Comments (all components):
GET /api/Comments/{baseId} → fetch comments (logged-in users)
POST /api/Comments/{baseId} → create comment (logged-in users)
DELETE /api/Comments/{id} → delete comment (admin only)

Requirements:

Navbar: navigate between components and config

List pages (/components/{component}):

Fetch all items

Display as clickable row cards:

Left: small image (/Photos/{Photo})

Middle: text (Brand + Model for GPU, etc.)

Right: Price (aligned right)

Entire row clickable → detail page

Admins: show Create button at top

Detail pages (/components/{component}/{id}):

Fetch component by id

Layout:

Center: vertical card with all attributes

Right: large image

Admin-only:

Edit form (photo + price only)

Delete button → call DELETE endpoint

Expandable comments section:

Fetch GET /api/Comments/{baseId}

Display comments as centered rectangles

Admin-only delete button per comment

Create comment form visible to all logged-in users

Deleting a comment removes it from local state

Generic / reusable:

Copilot should generate one generic pattern that works for all components

Do not hardcode component-specific names

Error & loading states:

Show “Loading…” while fetching

Show errors if fetch fails

Auth-based UI rules:

Only admins see edit/delete buttons

Everyone else read-only

Frontend routing:

Use React Router v6

Clicking a row navigates to detail page

Data fetching:

Use fetch API

No assumptions about backend endpoints beyond the normalized list above

UI styling:

Minimal, clean, white cards on dark background

Accent color for buttons: #13d4c8

Your task:
Generate fully functional list + detail pages + comment section for all components. Make it generic so I can reuse the same template. Include admin-only buttons as described and make it visually match the styling rules. Ensure all fetches respect the normalized backend endpoints.
